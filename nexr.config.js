// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['plus.unsplash.com', 'images.unsplash.com'], // Thêm các miền mà bạn sử dụng
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
